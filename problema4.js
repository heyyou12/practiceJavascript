let estudiantes= prompt("Ingrese la cantidad de estudiantes");
let cantidadEstudiantes = [];

for(i = 0; i <estudiantes; i++){

    cantidadEstudiantes [i] = [prompt("ingrese el nombre del estudiante"),0];
}

function tomarAsistencia(nombre,p){
    let asistio=prompt(nombre);
    
    if(nombre == "p" || nombre == "P"){

        cantidadEstudiantes[i]++;

    }

}

for(nombres of cantidadEstudiantes){

    document.write(nombres + "<br>");
}
